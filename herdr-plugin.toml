id = "dev.tupton.worktree-include"
name = "Worktree Include"
version = "0.4.0"
min_herdr_version = "0.7.0"
description = "Copy or symlink selected local files into new worktrees."
platforms = ["linux", "macos"]

[[events]]
on = "worktree.created"
command = ["bash", "src/include.sh"]
